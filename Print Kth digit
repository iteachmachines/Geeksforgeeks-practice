#include<bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
int t;
cin>>t;
while(t--)
{
int a,b,k;
cin>>a>>b>>k;
long long r,i;
r=pow(a,b);
while(k>0)
{
i=r%10;
r=r/10;
k--;
}
cout<<i<<endl;
}
return 0;
}
